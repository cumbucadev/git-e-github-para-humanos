---
icon: puzzle-piece
---

# Solução Exercício 3

**1) Crie a pasta `backup`:**

```bash
mkdir backup
```

**2) Mova o arquivo de backup para dentro da pasta:**

Linux/macOS:

```bash
mv anotacoes-backup.txt backup/
```

Windows:

```cmd
move anotacoes-backup.txt backup
```

**3) Liste os arquivos e pastas para conferir:**

Linux/macOS:

```bash
ls
```

Windows:

```cmd
dir
```

**4) Volte para a pasta acima:**

```bash
cd ..
```

**5) Confirme sua localização:**

```bash
pwd
```

**6) Remova a pasta `pratica-terminal` e todo o seu conteúdo:**

Linux/macOS:

```bash
rm -r pratica-terminal
```

Windows:

```cmd
rmdir /S pratica-terminal
```

Esse comando não gera saída, e a remoção é imediata.

***

🏁 Excelente! Você praticou navegação, criação, cópia, movimentação e remoção de arquivos e pastas usando o terminal.
