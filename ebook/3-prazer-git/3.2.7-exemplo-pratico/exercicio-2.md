---
icon: dumbbell
---

# Exercício 2

## Pedindo ajuda com `git help`

Como vimos, o Git possui um sistema de ajuda integrado. Saber usá-lo é mais importante do que decorar comandos.

#### ✅ Tarefa

Escolha um dos comandos abaixo (ou qualquer outro que tenha chamado sua atenção):

* `init`
* `clone`
* `status`

E execute:

```bash
git help comando
```

(substituindo `comando` pelo nome escolhido)

Na documentação que abrir, encontre:

* uma frase que explique **o que o comando faz**;
* pelo menos **uma opção** disponível;
* um **exemplo de uso**.

Em seguida, execute também:

```bash
git comando --help
```

ou:

```bash
git comando -h
```

#### 💡 Dica

Compare a saída do `git help comando` com a do `git comando --help`. Perceba como uma tende a ser mais completa e a outra mais resumida. Isso vai te ajudar a escolher qual usar no dia a dia.
